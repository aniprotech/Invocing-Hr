fastapi==0.115.0
# Pinned, though fastapi would pull it in anyway. It was not, so the
# same requirements file installed a different starlette depending on
# the day - and the version decides whether a request's database
# session is still open when a background task runs. CI and production
# were not testing the same software.
starlette==0.38.6
uvicorn[standard]==0.30.6
gunicorn==22.0.0
sqlalchemy==2.0.35
python-dotenv==1.0.1
authlib==1.3.1
httpx==0.27.2
itsdangerous==2.2.0
google-api-python-client==2.147.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1
sqladmin==0.19.0
aiofiles==24.1.0
python-multipart==0.0.9
psycopg2-binary==2.9.9
